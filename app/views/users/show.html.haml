%h1.page_header
  = link_to(h(@user.login), profile_url(:login=>@user.login))
  has suggested these names...

%ul#suggestions
  - @names.each do |name|
    %li
      %p.name
        = sanitize(name.name) 
      %p.name_info
        submitted by 
        = link_to(sanitize(name.user.login), profile_url(:login=>name.user.login))
        &bull;
        = link_to('like') 
        &bull;
        = link_to('dislike') 


/
  %p
    %b Login:
    = h @user.login
  %p
    %b Login count:
    = h @user.login_count
  %p
    %b Last request at:
    = h @user.last_request_at
  %p
    %b Last login at:
    = h @user.last_login_at
  %p
    %b Current login at:
    = h @user.current_login_at
  %p
    %b Last login ip:
    = h @user.last_login_ip
  %p
    %b Current login ip:
    = h @user.current_login_ip
  = link_to 'Edit', edit_account_path
